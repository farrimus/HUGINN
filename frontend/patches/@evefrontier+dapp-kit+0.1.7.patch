diff --git a/node_modules/@evefrontier/dapp-kit/graphql/client.ts b/node_modules/@evefrontier/dapp-kit/graphql/client.ts
index f22f83d..db86fae 100644
--- a/node_modules/@evefrontier/dapp-kit/graphql/client.ts
+++ b/node_modules/@evefrontier/dapp-kit/graphql/client.ts
@@ -1,6 +1,6 @@
 import {
-  getCharacterOwnerCapType,
   getCharacterPlayerProfileType,
+  getEveWorldPackageId,
   getSuiGraphqlEndpoint,
 } from "../utils/constants";
 import type {
@@ -232,12 +232,13 @@ export async function getObjectOwnerAndOwnedObjectsWithJson(
  * @returns Promise resolving to assembly and character data
  * @internal
  */
-export async function getObjectAndCharacterOwner(objectAddress: string) {
+export async function getObjectAndCharacterOwner(objectAddress: string, packageId?: string) {
+  const pkg = packageId ?? getEveWorldPackageId();
   return executeGraphQLQuery<GetObjectAndCharacterOwnerResponse>(
     GET_OBJECT_DYNAMICFIELD_CHARACTER_WITH_JSON,
     {
       objectId: objectAddress,
-      characterOwnerType: getCharacterOwnerCapType(),
+      characterOwnerType: `${pkg}::access::OwnerCap<${pkg}::character::Character>`,
     },
   );
 }
@@ -473,7 +474,7 @@ export async function getObjectsByType(
  *
  * @see {@link transformToAssembly} for converting raw data to typed Assembly
  */
-export async function getAssemblyWithOwner(assemblyId: string): Promise<{
+export async function getAssemblyWithOwner(assemblyId: string, packageId?: string): Promise<{
   moveObject: {
     contents: {
       json: Record<string, unknown>;
@@ -488,7 +489,7 @@ export async function getAssemblyWithOwner(assemblyId: string): Promise<{
   destinationGate: RawSuiObjectData | null;
 }> {
   try {
-    const result = await getObjectAndCharacterOwner(assemblyId);
+    const result = await getObjectAndCharacterOwner(assemblyId, packageId);
 
     if (!result.data?.object?.asMoveObject) {
       console.warn("[DappKit] getAssemblyWithOwner: Assembly not found");
diff --git a/node_modules/@evefrontier/dapp-kit/providers/SmartObjectProvider.tsx b/node_modules/@evefrontier/dapp-kit/providers/SmartObjectProvider.tsx
index e04132d..a03f567 100644
--- a/node_modules/@evefrontier/dapp-kit/providers/SmartObjectProvider.tsx
+++ b/node_modules/@evefrontier/dapp-kit/providers/SmartObjectProvider.tsx
@@ -14,7 +14,7 @@ import {
   DetailedSmartCharacterResponse,
 } from "../types";
 
-import { DEFAULT_TENANT, POLLING_INTERVAL } from "../utils/constants";
+import { DEFAULT_TENANT, POLLING_INTERVAL, TENANT_CONFIG, type TenantId } from "../utils/constants";
 import { getAssemblyWithOwner, MoveObjectData } from "../graphql";
 import {
   getObjectId,
@@ -29,8 +29,8 @@ import { SmartObjectContextType } from "../types";
  * @category Types
  */
 export type FetchObjectDataInput =
-  | { itemId: string; selectedTenant: string }
-  | { objectId: string };
+  | { itemId: string; selectedTenant: string; packageId?: string }
+  | { objectId: string; packageId?: string };
 
 /** @category Providers */
 export const SmartObjectContext = createContext<SmartObjectContextType>({
@@ -104,7 +104,7 @@ const SmartObjectProvider = ({ children }: { children: ReactNode }) => {
           assemblyOwner: characterInfo,
           energySource,
           destinationGate,
-        } = await getAssemblyWithOwner(objectId);
+        } = await getAssemblyWithOwner(objectId, 'packageId' in input ? input.packageId : undefined);
 
         if (!moveObject) {
           console.warn(
@@ -223,9 +223,10 @@ const SmartObjectProvider = ({ children }: { children: ReactNode }) => {
       return;
     }
 
+    const packageId = TENANT_CONFIG[selectedTenant as TenantId]?.packageId;
     const input: FetchObjectDataInput = isObjectIdDirect
-      ? { objectId: selectedObjectId }
-      : { itemId: selectedObjectId, selectedTenant };
+      ? { objectId: selectedObjectId, packageId }
+      : { itemId: selectedObjectId, selectedTenant, packageId };
 
     // Initial fetch
     fetchObjectData(input, true);
@@ -258,9 +259,10 @@ const SmartObjectProvider = ({ children }: { children: ReactNode }) => {
 
   const handleRefetch = useCallback(async () => {
     if (!selectedObjectId) return;
+    const packageId = TENANT_CONFIG[selectedTenant as TenantId]?.packageId;
     const input: FetchObjectDataInput = isObjectIdDirect
-      ? { objectId: selectedObjectId }
-      : { itemId: selectedObjectId, selectedTenant };
+      ? { objectId: selectedObjectId, packageId }
+      : { itemId: selectedObjectId, selectedTenant, packageId };
     await fetchObjectData(input, true);
   }, [selectedObjectId, selectedTenant, isObjectIdDirect, fetchObjectData]);
 
diff --git a/node_modules/@evefrontier/dapp-kit/utils/mapping.ts b/node_modules/@evefrontier/dapp-kit/utils/mapping.ts
index 4a6eda4..104d02b 100644
--- a/node_modules/@evefrontier/dapp-kit/utils/mapping.ts
+++ b/node_modules/@evefrontier/dapp-kit/utils/mapping.ts
@@ -1,7 +1,7 @@
 import { bcs } from "@mysten/sui/bcs";
 import { Assemblies, State } from "../types";
 import { deriveObjectID } from "@mysten/sui/utils";
-import { getEveWorldPackageId, getObjectRegistryType } from "./constants";
+import { getEveWorldPackageId, TENANT_CONFIG, TenantId } from "./constants";
 import { getSingletonObjectByType } from "../graphql/client";
 
 /**
@@ -62,20 +62,20 @@ export function getAssemblyType(typeRepr: string): Assemblies {
   return Assemblies.Assembly;
 }
 
-// Cache for the Registry address
-let objectRegistryAddress: string | null = null;
+// Per-package-ID cache (supports multiple tenants simultaneously)
+const registryAddressByPkg: Record<string, string> = {};
+
 /**
- * Fetches the AssemblyRegistry singleton address from the chain
- * Caches the result to avoid repeated queries
+ * Fetches the AssemblyRegistry singleton address for the given package ID.
+ * Results are cached per package so multiple tenants work in the same session.
  *
  * @category Utilities - Mapping
  */
-export async function getRegistryAddress(): Promise<string> {
-  if (objectRegistryAddress) {
-    return objectRegistryAddress;
-  }
+export async function getRegistryAddress(packageId?: string): Promise<string> {
+  const pkg = packageId ?? getEveWorldPackageId();
+  if (registryAddressByPkg[pkg]) return registryAddressByPkg[pkg];
 
-  const registryType = getObjectRegistryType();
+  const registryType = `${pkg}::object_registry::ObjectRegistry`;
   const result = await getSingletonObjectByType(registryType);
 
   const address = result.data?.objects?.nodes?.[0]?.address;
@@ -83,13 +83,17 @@ export async function getRegistryAddress(): Promise<string> {
     throw new Error(`ObjectRegistry not found for type: ${registryType}`);
   }
 
-  objectRegistryAddress = address;
+  registryAddressByPkg[pkg] = address;
   return address;
 }
 
 /**
- * Derives an object ID from an in-game item ID using the AssemblyRegistry
+ * Derives an object ID from an in-game item ID using the AssemblyRegistry.
+ * Uses TENANT_CONFIG to resolve the correct package ID for the given tenant,
+ * so the app works across all tenants without a build-time env var constraint.
+ *
  * @param itemId - The in-game item ID
+ * @param selectedTenant - The tenant name (e.g. "utopia", "stillness")
  * @returns The derived Sui object ID
  *
  * @category Utilities - Mapping
@@ -98,7 +102,9 @@ export async function getObjectId(
   itemId: string,
   selectedTenant: string,
 ): Promise<string> {
-  const registryAddress = await getRegistryAddress();
+  const packageId =
+    TENANT_CONFIG[selectedTenant as TenantId]?.packageId ?? getEveWorldPackageId();
+  const registryAddress = await getRegistryAddress(packageId);
 
   const bcsType = bcs.struct("TenantItemId", {
     id: bcs.u64(),
@@ -110,7 +116,7 @@ export async function getObjectId(
 
   const objectId = deriveObjectID(
     registryAddress,
-    `${getEveWorldPackageId()}::in_game_id::TenantItemId`,
+    `${packageId}::in_game_id::TenantItemId`,
     key,
   );
 
